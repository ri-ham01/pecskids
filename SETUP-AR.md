# دليل التشغيل السريع — PECS Kids Arabic

## كيف أضيف صوراً جديدة؟

### الطريقة السهلة (بدون قاعدة بيانات)

1. شغّل الموقع: `npm run dev`
2. افتح **http://localhost:3000/settings**
3. اضغط **إضافة صورة جديدة**
4. اختر:
   - **من الجهاز** — ارفع PNG أو JPG من حاسوبك
   - **رابط إنترنت** — الصق رابط صورة من الإنترنت
5. اكتب الاسم بالعربية (مثل: تفاحة)
6. اختر الفئة (طعام، أفعال، …)
7. اضغط **إضافة الصورة**

الصورة تظهر فوراً في **المكتبة** و **إنشاء الجمل**.

> **نصيحة:** استخدم صوراً بسيطة وواضحة بحجم أقل من 800 كيلوبايت.

---

## لا تحتاج قاعدة بيانات!

المشروع يعمل الآن **بدون PostgreSQL** وبدون تعديل الكود.
البيانات (50+ صورة رمزية عربية) مدمجة داخل المشروع.

---

## خطوة واحدة فقط — شغّل الموقع

### 1) افتح Terminal في Cursor
- اضغط `` Ctrl + ` `` (زر تحت Esc)
- أو: **Terminal → New Terminal**

### 2) اكتب هذين الأمرين:

```powershell
cd c:\Users\DELL\Desktop\pictoweb
npm run dev
```

### 3) افتح المتصفح

```
http://localhost:3000
```

**هذا كل شيء!** لا حاجة لقاعدة بيانات.

---

## جرّب الموقع

| ماذا تفعل | أين |
|-----------|-----|
| اضغط صورة وتكلم | `/board` |
| ابنِ جملة: أنا + أريد + ماء | `/builder` |
| احفظ الجملة | زر **حفظ** |
| شاهد المحفوظ | `/favorites` |

---

## ماذا يعمل بدون قاعدة بيانات؟

| ✅ يعمل | ❌ يحتاج قاعدة بيانات لاحقاً |
|---------|------------------------------|
| لوحة التواصل | لوحة الإدارة الكاملة |
| إنشاء الجمل + تحدث | تسجيل الدخول (Clerk) |
| المكتبة + البحث | سجل التواصل السحابي |
| المفضلة (محفوظة في المتصفح) | إضافة صور من لوحة Admin |

---

## الاستضافة على Vercel (بدون قاعدة بيانات)

1. ارفع المشروع على GitHub
2. ادخل [vercel.com](https://vercel.com) → **Add Project**
3. أضف متغير بيئة واحد فقط:

```
USE_DEMO_MODE=true
```

4. اضغط **Deploy**

الموقع سيعمل على الإنترنت مباشرة!

---

## متى تحتاج قاعدة بيانات؟

فقط إذا أردت:
- تسجيل دخول الوالدين
- حفظ البيانات في السحابة
- لوحة إدارة لإضافة صور جديدة

**للبدء والتجربة مع الأطفال — لا تحتاجها الآن.**

---

## مشاكل شائعة

### "npm run dev" لا يعمل
```powershell
npm install
npm run dev
```

### الصفحة فارغة
تأكد أن العنوان: `http://localhost:3000` (وليس https)

### لا يتكلم بالعربية
- استخدم Chrome أو Edge
- في الإعدادات → سرعة الكلام

---

## للمساعدة

إذا واجهت مشكلة، أرسل لقطة شاشة للخطأ.
